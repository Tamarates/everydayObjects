{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "outDir": "./dist", // Cambia esto según tu estructura de carpetas
    "target": "ESNext", // Cambia esto según tus necesidades
    "module": "ESNext" // O el módulo que necesites
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}
